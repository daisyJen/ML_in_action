import logRegres
from numpy import *
# dataArr, labelMat = logRegres.loadDataSet()
# #weights = logRegres.gradAscent(dataArr, labelMat)
# weights = logRegres.stoGradAscent1(dataArr, labelMat)
# logRegres.plotBestFit(weights)

logRegres.multiTest()